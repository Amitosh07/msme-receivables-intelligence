import { ArrowUpDown, ChevronLeft, ChevronRight, Search } from "./Icons";
import { Link, useNavigate } from "react-router-dom";
import type { Invoice, Prediction } from "../lib/types";
import { date, money, titleCase } from "../lib/format";
import { RiskBadge, StatusBadge } from "./Badges";
type Props = { invoices: Invoice[]; predictions: Prediction[]; page: number; pageSize: number; total: number; onPage: (page: number) => void; query: string; setQuery: (value: string) => void; status: string; setStatus: (value: string) => void; risk: string; setRisk: (value: string) => void; sort: "due_date" | "amount" | "invoice_date" | "uploaded"; setSort: (value: "due_date" | "amount" | "invoice_date" | "uploaded") => void; direction: "asc" | "desc"; setDirection: (value: "asc" | "desc") => void };
export function InvoiceTable({ invoices, predictions, page, pageSize, total, onPage, query, setQuery, status, setStatus, risk, setRisk, sort, setSort, direction, setDirection }: Props) { const navigate = useNavigate(); const predictionByInvoice = new Map(predictions.map((p) => [p.invoice_id, p])); const pages = Math.max(1, Math.ceil(total / pageSize)); return <section className="table-section"><div className="table-controls"><label className="search"><Search size={17} /><input value={query} onChange={(e) => { setQuery(e.target.value); onPage(1); }} placeholder="Search invoice number" aria-label="Search invoices" /></label><select value={status} onChange={(e) => { setStatus(e.target.value); onPage(1); }} aria-label="Filter payment status"><option value="">All payment states</option><option value="OPEN">Outstanding</option><option value="PAID">Paid</option></select><select value={risk} onChange={(e) => { setRisk(e.target.value); onPage(1); }} aria-label="Filter risk"><option value="">All risk levels</option><option value="HIGH">High risk</option><option value="MEDIUM">Medium risk</option><option value="LOW">Low risk</option><option value="UNAVAILABLE">Prediction unavailable</option></select><label className="sort">Sort <select value={sort} onChange={(e) => { setSort(e.target.value as Props["sort"]); onPage(1); }}><option value="uploaded">Recently uploaded</option><option value="due_date">Due date</option><option value="amount">Amount</option><option value="invoice_date">Invoice date</option></select><button className="direction" type="button" onClick={() => setDirection(direction === "desc" ? "asc" : "desc")} aria-label={direction === "desc" ? "Sort descending" : "Sort ascending"}><ArrowUpDown size={14} /></button></label></div><div className="table-wrap"><table><thead><tr><th>Invoice</th><th>Amount</th><th>Issued</th><th>Due</th><th>Payment</th><th>Processing</th><th>Late-payment risk</th><th>Expected payment</th></tr></thead><tbody>{invoices.map((invoice) => { const prediction = predictionByInvoice.get(invoice.id); const open = () => navigate(`/invoices/${invoice.id}`); return <tr key={invoice.id} className="invoice-row" role="link" tabIndex={0} aria-label={`Open invoice ${invoice.invoice_number}`} onClick={open} onKeyDown={(e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); open(); } }}><td><Link className="invoice-link" to={`/invoices/${invoice.id}`} onClick={(e) => e.stopPropagation()}>{invoice.invoice_number}</Link></td><td className="numeric">{money(invoice.amount, invoice.currency)}</td><td>{date(invoice.invoice_date)}</td><td>{date(invoice.due_date)}</td><td><StatusBadge value={invoice.payment_status} /></td><td><StatusBadge value={invoice.processing_status} /></td><td>{prediction ? <RiskBadge tier={prediction.risk_tier} score={prediction.risk_score} /> : <span className="muted">Unavailable</span>}</td><td>{prediction?.expected_payment_date ? <span><span className="predicted-label">Predicted</span>{date(prediction.expected_payment_date)}</span> : "—"}</td></tr>; })}</tbody></table></div><footer className="pagination"><span>{total} invoice{total === 1 ? "" : "s"}</span><div><button disabled={page === 1} onClick={() => onPage(page - 1)} aria-label="Previous page"><ChevronLeft size={17} /></button><span>Page {page} of {pages}</span><button disabled={page === pages} onClick={() => onPage(page + 1)} aria-label="Next page"><ChevronRight size={17} /></button></div></footer></section>; }
